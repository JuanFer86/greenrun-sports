import { useForm } from './useForm';
import { devices } from './devices';
import { useWindowSize } from './useWindowSize';
import { createCookie, readCookie, deleteCookie } from './cookies'
import { handleLike } from './handleLikes'

export{ useForm, devices, useWindowSize, createCookie, readCookie, deleteCookie, handleLike }